{
  "name": "kleanly-app",
  "version": "1.0.0",
  "main": "./App.js",
  "private": true,
  "scripts": {
    "dev": "cd project/project/project && rm -rf node_modules package-lock.json && npm install && EXPO_NO_TELEMETRY=1 npx expo start --tunnel --clear-cache",
    "start": "cd project/project/project && rm -rf node_modules package-lock.json && npm install && EXPO_NO_TELEMETRY=1 npx expo start --tunnel -c --clear-cache"
  },
  "dependencies": {
    "expo": "~53.0.20",
    "react": "18.3.1",
    "react-native": "0.79.5"
  }
}
